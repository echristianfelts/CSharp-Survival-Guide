using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FvM_PositionMattersChallenge : MonoBehaviour
{
    //Create an array of 5 different positions and uses a custom method to generate a random index,
    //then use a custom methodto set the position to that subindex...
    //

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
